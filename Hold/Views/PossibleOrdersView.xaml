<UserControl x:Class="Hold.Views.PossibleOrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hold.Views"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="333">
    <Grid Grid.Column="1">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Margin="10 30 50 20"/>

            <UniformGrid Grid.Column="2" Rows="1" Margin="10 30 10 30">
                <Button Margin="0 0 10 0"/>
                <TextBlock/>
                <Button Margin="0 0 10 0"/>
            </UniformGrid>
        </Grid>

        <ListBox Grid.Row="1" Margin="10">

        </ListBox>
    </Grid>
</UserControl>
